# ──────────────────────────────────────────────
# Smart Retail Surveillance Backend
# Recommended Python version: 3.11.x (most stable)
# Install: pip install -r requirements.txt
# ──────────────────────────────────────────────

# ── Web Framework ──
fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2

# ── Database ──
sqlalchemy==2.0.35

# ── File Handling ──
python-multipart==0.0.9
aiofiles==24.1.0

# ── AI / ML (PyTorch) ──
# Using >= so existing CUDA installs (torch 2.5.1+cu121, etc.) are preserved
torch>=2.4.1
torchvision>=0.19.1
numpy>=1.26.4

# ── Image / Video Processing ──
opencv-python-headless==4.10.0.84
Pillow==10.4.0

# ── WebSocket ──
websockets==12.0
